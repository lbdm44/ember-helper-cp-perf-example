<div class="display-flex">
  <div class="display-flex flex-column">
    <ul class="fixed-width">
      {{#each model as |card|}}
        {{simple-card card=card}}
      {{/each}}
    </ul>
  </div>
  <div class="display-flex flex-column">
    <ul class="fixed-width">
      {{#each model as |card|}}
        {{simple-cp-card card=card}}
      {{/each}}
    </ul>
  </div>
</div>
